Make exactly these scoped changes to the public website (artifacts/hk-masters-web), nothing else — do not touch any other files, content, or behavior:

1. In artifacts/hk-masters-web/src/pages/Home.jsx, find:
   const ROTTERDAM_MODE_END = new Date("2026-09-15T00:00:00");
   Change the date string to "2026-08-02T00:00:00" (so Rotterdam mode is now off, since the tournament ended 1 Aug 2026). Leave ROTTERDAM_START and everything else in this file unchanged.

2. In artifacts/hk-masters-web/src/pages/Teams.jsx, find the identical line:
   const ROTTERDAM_MODE_END = new Date("2026-09-15T00:00:00");
   Make the same change: "2026-08-02T00:00:00".

3. Still in Teams.jsx, find this block (the "View Squad List" button, currently only shown when rotterdamMode is true):
   {rotterdamMode && (
     <button
       onClick={() => setOpenSquad({ category: squad.short_name, teamInfo: squad })}
       className="btn-shimmer inline-flex items-center gap-2 bg-[#1E3A6E] text-white font-semibold px-6 py-2.5 rounded-lg hover:bg-[#16305D] transition-colors duration-150 text-sm"
     >
       View Squad List →
     </button>
   )}
   Remove the `rotterdamMode &&` condition so this button always renders regardless of rotterdamMode (keep everything else about it identical — same onClick, same className, same text).

4. Still in Teams.jsx, in the bottom CTA section, find the non-Rotterdam-mode branch:
   <Link
     href="/contact"
     className="btn-shimmer inline-block bg-[#DE2910] text-white font-semibold px-8 py-3 rounded-lg hover:bg-red-700 transition-colors duration-150"
   >
     Contact Us to Join
   </Link>
   Replace it with a plain external anchor tag (not the internal Link component, since this is now an external URL) pointing to our Notion sign-up form, opening in a new tab:
   
     href="https://caramel-havarti-6da.notion.site/79a429c0d2cc4ccb96417607a58775f9?pvs=105"
     target="_blank"
     rel="noopener noreferrer"
     className="btn-shimmer inline-block bg-[#DE2910] text-white font-semibold px-8 py-3 rounded-lg hover:bg-red-700 transition-colors duration-150"
   >
     Sign Up to Join →
   </a>

After making these 4 changes exactly as specified, commit them and push to GitHub so the Netlify build picks them up, following the project's documented deploy flow in README.md: git pull -X ours origin main, then git push origin main. Report back the exact diff of what changed and confirm the push succeeded.